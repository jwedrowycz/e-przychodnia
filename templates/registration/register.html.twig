{% extends 'base.html.twig' %}

{% block title %}Rejestracja{% endblock %}

{% block body %}
    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.password) }}
        {{ form_row(registrationForm.imie) }}
        {{ form_row(registrationForm.nazwisko) }}
        {{ form_row(registrationForm.PESEL) }}
        {{ form_row(registrationForm.telefon) }}
        {{ form_row(registrationForm.adres_zamieszkania) }}
        {{ form_row(registrationForm.miasto) }}
        {{ form_row(registrationForm.kod_pocztowy) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}
