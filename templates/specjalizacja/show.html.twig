{% extends 'base.html.twig' %}

{% block title %}Specjalizacja{% endblock %}

{% block body %}
    <h1>Specjalizacja</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specjalizacja.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ specjalizacja.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('specjalizacja_index') }}">back to list</a>

    <a href="{{ path('specjalizacja_edit', {'id': specjalizacja.id}) }}">edit</a>

    {{ include('specjalizacja/_delete_form.html.twig') }}
{% endblock %}
