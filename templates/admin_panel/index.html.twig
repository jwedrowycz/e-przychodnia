{% extends 'base.html.twig' %}

{% block title %}Panel administratora{% endblock %}

{% block body %}
<div class="admin-grid-container">
    {% block sidebar %}
        <div class="sidebar">
        <h3>Panel administratora</h3>
            <a {% if app.request.attributes.get('_route') in [
                'admin.users',
                'admin.add_user',
                'admin.users_only',
                ] %} 
                class="active"{% endif %} href="{{ path('admin.users') }}">Użytkownicy</a>
            <a {% if app.request.attributes.get('_route') in [
                'admin.clinics_show',
                'admin.clinic_add',
                ] %} 
                class="active"{% endif %}  href="{{ path('admin.clinics_show') }}">Poradnie</a>
                <a {% if app.request.attributes.get('_route') in [
                'admin.unit',
                'admin.unit_show',
                'admin.unit_add',
                ] %} 
                class="active"{% endif %}  href="{{ path('admin.unit') }}">Jednostka</a>
            <a {% if app.request.attributes.get('_route') in [
                'admin.doctors',
                'admin.doctor_add',
                ] %}
                class="active"{% endif %}  href="{{ path('admin.doctors') }}">Lekarze</a>
            <a {% if app.request.attributes.get('_route') in [
                'admin.visits',
                ] %}
                class="active"{% endif %}  href="{{ path('admin.visits') }}">Wizyty</a>
        </div>
    {% endblock sidebar %}
    <div class="content">
        {% block content %}
        {% endblock content %}
  </div>   
</div>
{% endblock body %}
