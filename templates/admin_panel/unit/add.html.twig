{% extends 'admin_panel/index.html.twig' %}

{% block content %}
     <h1>{{ clinic.nazwa }}</h1>
     <h3>Dodaj lekarza do poradni</h3>
     <table class="admin-table">
          <tr>
               <th>Numer PWZ</th>
               <th>Imie i nazwisko</th>
               <th>Specjalizacja</th>
               <th></th>
          </tr>
          {% for d in doctors %}
               <tr>
                    <td>{{ d.numPwz }}</td>
                    <td>{{ d.lastName ~ ' ' ~ d.name }}</td>
                    <td>{{ d.spec }}</td>
                    <td>
                         <a class="add" href="{{ path('admin.unit_create', {idDoctor: d.id, idCLinic: clinc.id}) }}">Dodaj</a>
                    </td>
               </tr>
          {% endfor %}
     </table>
{% endblock content %}
