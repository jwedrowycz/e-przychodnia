{% extends 'admin_panel/index.html.twig' %}

{% block content %}
    <h1>Dodaj poradnie</h1>
    {{ form_start(addPoradniaForm) }}
    {{ form_row(addPoradniaForm.nazwa)}}
    {{ form_row(addPoradniaForm.zapisz) }}
    {{ form_end(addPoradniaForm) }}

{% endblock %}
