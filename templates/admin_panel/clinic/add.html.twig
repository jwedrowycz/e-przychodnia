{% extends 'admin_panel/index.html.twig' %}

{% block content %}
    <h1>Dodaj poradnie</h1>
    {{ form_start(form) }}
    {{ form_row(form.name)}}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}

{% endblock %}
